import axios, {CancelToken} from "axios";

export class UserService {
    static async getUsers(token?: CancelToken) {
        try {
            //const config =
           // const users = await axios.get('')
        } catch (e: unknown) {
            return null
        }
    }
}